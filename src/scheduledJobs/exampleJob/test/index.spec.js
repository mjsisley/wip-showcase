/* eslint-disable */
const handler = require("../src/index");
const mockS3PutEvent = require("./fixtures/events/s3.put.json");

describe("updateUserInfo Lambda Function", () => {
  test("", () => {
    expect(true).toBe(true);
  });
});
